# Database Configuration
DATABASE_URL=postgres://username:password@postgres:5432/portfolio
PGHOST=postgres
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_secure_password
PGDATABASE=portfolio

# App Configuration
NODE_ENV=production
PORT=5000
SESSION_SECRET=your_session_secret_key_should_be_at_least_32_chars

# OpenAI API (if needed)
OPENAI_API_KEY=your_openai_api_key

# JWT Configuration (for authentication)
JWT_SECRET=your_jwt_secret_key_should_be_at_least_32_chars
JWT_EXPIRES_IN=24h

# Domain Configuration (for production)
APP_DOMAIN=yourdomain.com